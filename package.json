{
  "name": "rupe",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:push": "pnpm --filter @rupe/database db:push",
    "db:studio": "pnpm --filter @rupe/database db:studio",
    "db:generate": "pnpm --filter @rupe/database db:generate"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
